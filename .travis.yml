---
sudo: false
dist: trusty
language: ruby
cache: bundler
  - rm -f Gemfile.lock
  - gem update --system
  - gem --version
  - bundle -v
script:
  - 'bundle exec rake $CHECK'
bundler_args: --without system_tests
rvm:
  - 2.5.1
env:
  global:
    - PUPPET6=debian9 PUPPET_GEM_VERSION="~> 6.0"
matrix:
  fast_finish: true
  include:
    -
      bundler_args:
      dist: trusty
      env: PUPPET6=scientificlinux
      script:
      - bundle exec rake 'litmus:provision[docker, waffleimage/scientificlinux6]'
      - bundle exec rake 'litmus:provision[docker, waffleimage/scientificlinux7]'
      - bundle exec rake litmus:install_agent
      - bundle exec rake litmus:install_module
      - travis_wait 60 bundle exec rake litmus:acceptance:parallel
      services: docker
      sudo: required
    -
      bundler_args:
      dist: trusty
      env: PUPPET6=oracle
      script:
      - bundle exec rake 'litmus:provision[docker, waffleimage/oraclelinux6]'
      - bundle exec rake 'litmus:provision[docker, waffleimage/oraclelinux7]'
      - bundle exec rake litmus:install_agent
      - bundle exec rake litmus:install_module
      - travis_wait 60 bundle exec rake litmus:acceptance:parallel
      services: docker
      sudo: required
    -
      bundler_args:
      dist: trusty
      env: PUPPET6=centos6
      script:
      - bundle exec rake -T
      - bundle exec rake 'litmus:provision[docker, waffleimage/centos6]'
      - bundle exec rake litmus:install_agent
      - bundle exec rake litmus:install_module
      - travis_wait 60 time TARGET_HOST=localhost:2222 bundle exec rspec ./spec/acceptance/
      services: docker
      sudo: required
    -
      bundler_args:
      dist: trusty
      env: PUPPET6=centos7
      script:
      - bundle exec rake -T
      - bundle exec rake 'litmus:provision[docker, waffleimage/centos7]'
      - bundle exec rake litmus:install_agent
      - bundle exec rake litmus:install_module
      - travis_wait 60 time TARGET_HOST=localhost:2222 bundle exec rspec ./spec/acceptance/
      services: docker
      sudo: required

    -
      env: CHECK="syntax lint metadata_lint check:symlinks check:git_ignore check:dot_underscore check:test_file rubocop"
    -
      env: CHECK=parallel_spec
    -
      env: PUPPET_GEM_VERSION="~> 5.0" CHECK=parallel_spec
      rvm: 2.4.4
    -
      bundler_args:
      dist: trusty
      env: PUPPET6=travis_deb
      script:
      - bundle exec rake 'litmus:provision_list[travis_deb]'
      - bundle exec bolt command run 'apt-get install wget -y' --inventoryfile inventory.yaml --nodes='localhost*'
      - bundle exec rake litmus:install_agent
      - bundle exec rake litmus:install_module
      - travis_wait 40 bundle exec rake litmus:acceptance:parallel
      services: docker
      sudo: required
branches:
  only:
    - master
    - /^v\d/
    - release
notifications:
  email: false
deploy:
  provider: puppetforge
  user: puppet
  password:
    secure: ""
  on:
    tags: true
    all_branches: true
    condition: "$DEPLOY_TO_FORGE = yes"
